apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-db.fullname" . }}-test-connection"
  labels:
{{ include "user-db.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "user-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
