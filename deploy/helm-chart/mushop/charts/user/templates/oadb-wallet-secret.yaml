apiVersion: v1
kind: Secret
metadata:
  name: {{ include "user.fullname" . }}-wallet
  labels: {{ include "user.labels" . | nindent 4 }}
type: Opaque
data:
{{- (.Files.Glob "secrets/Wallet*/*").AsSecrets | nindent 2 }}